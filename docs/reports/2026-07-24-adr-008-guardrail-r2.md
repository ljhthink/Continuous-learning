# 安全与质量审计报告 · ADR-008 第二轮审查（R2）

> 本报告由 `guardrail-enforcer` 子 Agent 产出，针对 ADR-008《知识库内容分层与格式统一》第一轮审查阻断项（B-1/B-2/B-3）与建议项（S-1~S-5）的修复进行验证审查。
> 审查策略：逐项验证修复正确性 + 检查修复是否引入新问题 + 核实修复新增的源码断言。不重复第一轮已完成的完整源码核实。

## 元信息

| 项目 | 内容 |
| --- | --- |
| 执行 Agent | guardrail-enforcer |
| 任务令牌 | TKN-ADR-008-002 |
| 任务域 | adr-008（知识库内容分层与格式统一·第二轮） |
| 报告日期 | 2026-07-24 |
| 审查范围 | `docs/decisions/ADR-008-kb-content-layering-and-format-unification.md`（修复后，162 行）、`docs/decisions/README.md`（索引行）、第一轮报告 `docs/reports/2026-07-24-adr-008-guardrail.md` |
| 风险等级 | P0 微小（本 PR 为纯 ADR 文档修订，无代码变更；ADR 内描述的决策执行风险为 P2） |
| 第一轮报告路径 | `docs/reports/2026-07-24-adr-008-guardrail.md`（TKN-ADR-008-001） |
| 主 Agent 签发上下文 | 盲区 1：S-2 依赖理由修改是否准确（已核实：准确，迁移手写文档时手动遵循新规范）；盲区 2：S-3 新增"归属"列后表格列数变化是否触发 markdownlint（已核实：0 issues）；遗憾 1：未在 ADR 中明确 kb-system 的 frontmatter domain 字段值（Q-1 仍待澄清）；遗憾 2：DEF-010 未标注"可分批执行"（Q-2 部分回应） |

## 1. 审查依据

- 本次变更：PR #17 第二轮修订，修复第一轮 3 项阻断项 + 5 项建议项
- 影响自检结果：主 Agent 提供的自检表（纯文档变更，无接口/契约/依赖变更）
- 相关 ADR：ADR-001（技术栈）、ADR-006（持续进化闭环）
- 第一轮报告结论：有条件通过，需修复 B-1/B-2/B-3 后重新提交
- 审查规范：CLAUDE.md §17.4（ADR 评审流程）、§16（风险分级）、§20.4（任务令牌）
- 源码核实对象（仅限修复新增断言）：`server/src/tools/read-only.ts`、`server/src/tools/write.ts`、`server/src/tools/search.ts`
- 约束：本 PR 为纯 ADR 文档变更，无代码变更，安全审计项（OWASP/CWE/注入/密钥）均为 N/A

## 2. 第一轮阻断项修复验证

### B-1：决策 1 备选方案标注矛盾

| 项目 | 内容 |
| --- | --- |
| 第一轮问题 | "修 MCP server 代码（选定）"与"两者都做（最终选定）"同时标注"选定" |
| 修复要求 | 第一行去掉"选定"标注，仅保留"两者都做（选定）" |
| 修复位置 | ADR-008 第 87-91 行，备选方案表"决策 1 备选" |
| 验证结论 | ✅ 已修复 |

修复后表格内容（ADR-008 第 88-91 行）：

```markdown
| 修 MCP server 代码 | 根治，未来自动合规 | 不清理存量，4 张旧卡片仍是旧格式 |
| 仅手动修复 4 张卡片 | 不动代码 | 下次 `kb_write_experience` 还会生成旧格式，治标不治本 |
| 两者都做（选定） | 根治 + 清理存量 | 工作量最大但最彻底 |
```

核实结论：第一行"修 MCP server 代码"已去掉"选定"标注，仅"两者都做（选定）"保留"选定"。标注矛盾已消除。

### B-2：参考部分循环引用

| 项目 | 内容 |
| --- | --- |
| 第一轮问题 | 参考第一条 file:/// 链接指向 ADR 自身，形成循环引用 |
| 修复要求 | 改为指向真实会话记录路径，或删除此条参考 |
| 修复位置 | ADR-008 第 157-162 行，参考部分 |
| 验证结论 | ✅ 已修复 |

修复后参考部分内容（ADR-008 第 159-162 行）：

```markdown
- AGENTS.md §3（frontmatter Schema）、§4.2（Ingest 工作流）、§8.1（领域目录）、§8.3（新建领域）
- `server/src/utils/frontmatter.ts`（serializeFrontmatter 实现）
- `server/src/tools/write.ts`（kbWriteExperience、kbPromoteExperience）
- Route B PR #16（触发本次反思的变更）
```

核实结论：循环引用已删除。参考部分不再包含指向 ADR 自身的 file:/// 链接。4 条参考均指向有效外部文档/代码/PR，无循环引用。所有链接均使用相对路径或纯文本引用，不含 file:/// 绝对路径，符合主 Agent "避免 Linux CI 失败"的约束。

### B-3：serializeFrontmatter 影响面评估不完整

| 项目 | 内容 |
| --- | --- |
| 第一轮问题 | serializeFrontmatter 有 5 个调用点（含高频 kb_get_page），ADR 仅聚焦 experiences，未要求 DEF-008 回归测试覆盖所有调用方 |
| 修复要求 | ① 决策 1 补充 5 个调用点；② DEF-008 任务描述补充回归测试覆盖全部 5 个调用方 |
| 修复位置 | ADR-008 决策 1（第 57-65 行） |
| 验证结论 | ✅ 已修复（附 2 项低风险行号偏差，见 §5） |

修复后决策 1 影响面段落（ADR-008 第 57-65 行）：

```markdown
**影响面（经 guardrail-enforcer 源码核实）**：`serializeFrontmatter` 有 **5 个调用点**，不限于 experiences 卡片：

1. `kb_get_page` 的 `use_count` 写回（[read-only.ts:209](../../server/src/tools/read-only.ts#L209)，高频调用）
2. `kb_ingest_source`（[write.ts:114](../../server/src/tools/write.ts#L114)）
3. `kb_write_experience`（[write.ts:188](../../server/src/tools/write.ts#L188)）
4. `kb_promote_experience` promote（[write.ts:297](../../server/src/tools/write.ts#L297)）
5. `kb_promote_experience` reject（[write.ts:328](../../server/src/tools/write.ts#L328)）

修改引入 bug 会影响 `kb_get_page` 高频写回路径，可能导致大范围页面格式损坏。DEF-008 必须回归测试覆盖全部 5 个调用方。
```

核实结论：

- ✅ 5 个调用点已完整列出，含高频 kb_get_page 的风险说明
- ✅ DEF-008 回归测试要求已明确（"DEF-008 必须回归测试覆盖全部 5 个调用方"）
- ✅ 调用点使用相对路径链接（`../../server/src/...`），符合 CI 兼容要求
- ⚠️ 2 个行号存在 1 行偏差（见 §5 源码引用准确性核实）

## 3. 第一轮建议项修复验证

### S-1：背景问题 2 决策回应缺失

| 项目 | 内容 |
| --- | --- |
| 第一轮建议 | 在决策部分增加说明"问题 2 无需新决策，现有机制保留" |
| 修复位置 | ADR-008 第 45-51 行 |
| 验证结论 | ✅ 已采纳 |

新增"决策 0：新领域机制维持现状（针对背景问题 2）"，明确说明现有 AGENTS.md §4.2 + §8.3 机制保留，无需新决策。背景问题 2 与决策部分的结构缺口已消除。

决策编号顺序检查：决策 0（维持现状）→ 决策 1（格式统一）→ 决策 2（领域拆分）→ 决策 3（知识深化）。编号 0/1/2/3 逻辑清晰，0 表示"元决策（维持现状）"，1/2/3 表示实际变更决策，无歧义。

### S-2：DEF-009 依赖理由表述不精确

| 项目 | 内容 |
| --- | --- |
| 第一轮建议 | 将"避免二次修复"改为"格式规范确立后再迁移，确保迁移时遵循新规范" |
| 修复位置 | ADR-008 第 143 行，DEF-009 依赖列 |
| 验证结论 | ✅ 已采纳 |

修复后依赖理由：`DEF-008（格式规范确立后再迁移，确保迁移时遵循新规范）`。

主 Agent 自问中对此修复的担忧（"DEF-009 迁移的是手写文档，不触发 serializeFrontmatter"）经核实不构成问题：DEF-009 迁移手写文档时，Agent 需手动修改 frontmatter，此时应遵循决策 1 确立的新格式规范（单行数组 + 无引号 date + 空行）。因此"确保迁移时遵循新规范"的表述准确，依赖理由成立。

### S-3：后续任务清单聚焦度

| 项目 | 内容 |
| --- | --- |
| 第一轮建议 | 将 DEF-007/001/006 标注为"顺带列入的非本 ADR 任务" |
| 修复位置 | ADR-008 第 136-146 行，任务清单表 |
| 验证结论 | ✅ 已采纳 |

新增"归属"列，区分四类归属：

- "本 ADR 决策 1/2/3"——本 ADR 直接产出任务（DEF-008/009/010）
- "顺带列入（既有技术债）"——DEF-007、DEF-006
- "顺带列入（既有安全债）"——DEF-001
- "运维操作"——重启 MCP server

表格列数从 5 列变为 6 列。markdownlint 检查结果：0 issues（见 §5），未触发 MD056（表格列数不一致）或其他格式规则。主 Agent 自问中对表格格式的担忧已排除。

### S-4：DEF-010 工作量估算偏乐观

| 项目 | 内容 |
| --- | --- |
| 第一轮建议 | 将"1-2 小时"改为"1-4 小时（估算，视算法复杂度而定）" |
| 修复位置 | ADR-008 第 122 行，负面后果 |
| 验证结论 | ✅ 已采纳 |

修复后表述：`每个算法 concept 页需 1-4 小时估算`。工作量估算区间已扩大，缓解了乐观偏差。此外，执行顺序建议第 154 行已标注 DEF-010 为"长期任务"，部分回应了 Q-2 待澄清项。

### S-5：决策 2 补充 MCP 工具动态扫描说明

| 项目 | 内容 |
| --- | --- |
| 第一轮建议 | 补充"所有 MCP 工具基于动态扫描，新建 kb-system 领域无需修改 server 代码" |
| 修复位置 | ADR-008 第 73 行，决策 2 "MCP 工具兼容性"段落 |
| 验证结论 | ✅ 已采纳 |

新增"MCP 工具兼容性（经 guardrail-enforcer 源码核实）"段落，引用 search.ts:69-75、read-only.ts:80-85，说明 kb_search 和 kb_list_categories 均基于动态扫描，新建 kb-system 领域无需修改 MCP server 代码。消除了读者对"新建领域是否需改代码"的疑虑。

## 4. 修复引入新问题检查

| 检查项 | 结论 | 说明 |
| --- | --- | --- |
| markdownlint 合规 | ✅ 通过 | 运行 `npx markdownlint-cli2` 对 ADR-008 检查，结果 0 issues。新增"归属"列（5→6 列）未触发 MD056 |
| 决策编号顺序 | ✅ 通过 | 决策 0/1/2/3 顺序清晰，0 为元决策（维持现状），1/2/3 为实际变更，无歧义 |
| 新增"决策 0"与背景问题 2 的对应 | ✅ 通过 | 决策 0 显式标注"针对背景问题 2"，背景三问题与决策部分一一对应（问题 1→决策 1，问题 2→决策 0，问题 3→决策 2+3） |
| 参考链接格式 | ✅ 通过 | 全部使用相对路径或纯文本，无 file:/// 绝对路径，CI 兼容 |
| 表格列数一致性 | ✅ 通过 | 任务清单表 6 列（编号/任务/归属/风险等级/依赖/状态），所有行列数一致 |
| 链接可达性 | ✅ 通过 | ADR 内相对链接 `../../server/src/...` 指向真实存在的源码文件（已核实） |

## 5. 源码引用准确性核实

本轮对 B-3 修复新增的 5 个调用点行号 + S-5 修复引用的 2 处源码位置进行逐行核实（本 PR 无代码变更，源码行号应与第一轮一致）。

### 5.1 决策 1 的 5 个调用点

| ADR 引用 | 实际行号 | 实际代码 | 结论 |
| --- | --- | --- | --- |
| read-only.ts:209 | 209 | `await writeFile(fullPath, serializeFrontmatter(frontmatter, body));` | ✅ 准确 |
| write.ts:114 | 114 | `await writeFile(wikiFullPath, serializeFrontmatter(frontmatter, body));` | ✅ 准确 |
| write.ts:188 | 188 | `await writeFile(inboxFullPath, serializeFrontmatter(frontmatter, content));` | ✅ 准确 |
| write.ts:297 | **296** | 第 296 行：`await writeFile(activeFullPath, serializeFrontmatter(frontmatter, body));`；第 297 行：`// Remove from inbox now that it lives in the active location.`（注释） | ⚠️ 偏差 1 行 |
| write.ts:328 | **327** | 第 327 行：`await writeFile(fullPath, serializeFrontmatter(frontmatter, body));`；第 328 行：空行 | ⚠️ 偏差 1 行 |

偏差分析：

- 2 个行号各偏差 1 行，ADR 引用的行号指向实际调用点的**下一行**（注释行 / 空行）
- 读者点击链接后能看到上一行即为 `serializeFrontmatter` 调用，上下文清晰，不会造成误解
- 偏差来源：主 Agent 修复 B-3 时直接引用了第一轮报告的行号（第一轮报告第 185 行同样写 297/328），第一轮审查未发现此偏差
- 严重度：**低风险**——不影响决策逻辑正确性，不影响读者理解，不在 CLAUDE.md §17.4 阻断标准内

### 5.2 决策 2 的 MCP 工具引用

| ADR 引用 | 实际行号 | 实际代码 | 结论 |
| --- | --- | --- | --- |
| search.ts:69-75 | 69-75 | 第 69 行注释 `// Domain filter`，第 70-74 行 domain 过滤逻辑，第 75 行 `}` 闭合 | ✅ 准确 |
| read-only.ts:80-85 | 80-85 | 第 80 行 `let domains`，第 82-85 行 `fs.readdir` 动态扫描 | ✅ 准确 |

## 6. 安全漏洞扫描（TRAE-security-review）

> 本 PR 为纯 ADR 文档修订，无代码变更，Stage 2-5 安全审计项均为 N/A。

| 审计项 | 结论 | 说明 |
| --- | --- | --- |
| OWASP Top 10 / CWE | N/A | 纯文档变更，无可执行代码 |
| 输入与边界审计 | N/A | 纯文档变更 |
| 执行安全审计（注入防护） | N/A | 纯文档变更 |
| 密钥与配置安全 | ✅ 通过 | 扫描 ADR-008 全文，无硬编码密钥、密码、令牌、API Key、内部 IP/域名 |
| 依赖与供应链风险 | N/A | 无依赖描述文件变更 |
| 已知未修复安全缺陷追踪 | ✅ 记录 | DEF-001（TOCTOU 竞态）仍在任务清单中追踪，状态"待开始"，属既有安全债，不属于本 PR 变更范围，不阻断 |

## 7. 待澄清项跟踪

| 编号 | 待澄清项 | 第一轮状态 | 本轮状态 | 说明 |
| --- | --- | --- | --- | --- |
| Q-1 | kb-system 领域的 frontmatter `domain` 字段值应统一为 `kb-system` 还是 `meta`？ | 待主 Agent 澄清 | ⚠️ 仍待澄清 | ADR-008 未明确此值。建议在 DEF-009 执行前明确，避免迁移时 frontmatter 修改不一致。**不阻断本 ADR 合并**（属执行阶段细节，不影响决策本身） |
| Q-2 | DEF-010 的"高成本"未量化，是否标注"可分批执行" | 待主 Agent 澄清 | ⚠️ 部分回应 | 工作量已改为"1-4 小时估算"，执行顺序建议已标注"长期任务"。但未显式标注"可分批执行"。**不阻断本 ADR 合并**（DEF-010 为 P2 长期任务，执行时可自然分批） |

## 8. 综合结论

- [x] **通过**：可进入测试阶段
- [ ] **有条件通过**：需修复阻断项后重新提交
- [ ] **阻断**：存在严重质量缺陷或高危安全漏洞

### 通过判定依据（CLAUDE.md §17.4）

| 审查维度 | 结论 | 说明 |
| --- | --- | --- |
| 决策逻辑一致性 | ✅ 通过 | 三决策 + 决策 0 逻辑自洽，背景问题与决策一一对应，无矛盾 |
| 备选方案充分性 | ✅ 通过 | 每个决策均有 3-4 个备选，标注矛盾已消除（B-1 修复），否决理由合理 |
| 后果完整性 | ✅ 通过 | serializeFrontmatter 5 调用点影响面已完整补充（B-3 修复），DEF-008 回归测试要求已明确，MCP 工具兼容性已说明（S-5 修复） |
| 模板结构 | ✅ 通过 | 含背景/决策/备选/后果/参考五大必备章节，决策编号 0/1/2/3 清晰 |
| 风险等级 | ✅ 通过 | P2 跨模块合理 |
| 状态 | ✅ 通过 | Proposed，合并后转 Accepted |
| 任务令牌 | ✅ 通过 | TKN-ADR-008-002 符合格式 |
| markdownlint | ✅ 通过 | 0 issues |
| ADR 索引 | ✅ 通过 | `docs/decisions/README.md` 已追加 ADR-008 行，状态 Proposed |
| 安全审计 | ✅ N/A | 纯文档变更，无安全风险 |

### 阻断项修复汇总

| 阻断项 | 第一轮状态 | 本轮验证 | 结论 |
| --- | --- | --- | --- |
| B-1 备选方案标注矛盾 | 阻断 | 已修复 | ✅ 通过 |
| B-2 参考部分循环引用 | 阻断 | 已修复 | ✅ 通过 |
| B-3 serializeFrontmatter 影响面不完整 | 阻断 | 已修复（含 2 项低风险行号偏差） | ✅ 通过 |

### 建议项修复汇总

| 建议项 | 第一轮状态 | 本轮验证 | 结论 |
| --- | --- | --- | --- |
| S-1 问题 2 决策回应缺失 | 建议 | 已采纳（新增决策 0） | ✅ 通过 |
| S-2 DEF-009 依赖理由不精确 | 建议 | 已采纳 | ✅ 通过 |
| S-3 任务清单聚焦度 | 建议 | 已采纳（新增归属列） | ✅ 通过 |
| S-4 DEF-010 工作量偏乐观 | 建议 | 已采纳 | ✅ 通过 |
| S-5 决策 2 缺 MCP 工具说明 | 建议 | 已采纳 | ✅ 通过 |

## 9. 低风险建议项（不阻断合并）

**L-1：决策 1 调用点行号偏差（2 处）**

- 位置：ADR-008 第 63-64 行
- 问题：`write.ts:297` 实际应为 `write.ts:296`（297 行为注释），`write.ts:328` 实际应为 `write.ts:327`（328 行为空行）
- 严重度：低风险——偏差仅 1 行，指向实际调用点的下一行，读者能轻易定位，不影响理解
- 建议：在后续 DEF-008 执行或下次 ADR 修订时修正这两处行号。非紧急，不阻断本 ADR 合并。

## 10. 最终声明

**ADR-008 可合并，状态可从 Proposed 转 Accepted。**

第一轮 3 项阻断项（B-1/B-2/B-3）已全部正确修复，5 项建议项（S-1~S-5）已全部采纳。修复未引入新的阻断级问题。markdownlint 0 issues。仅存在 1 项低风险建议项（L-1，2 处行号偏差 1 行），不构成阻断。

待澄清项 Q-1（kb-system frontmatter domain 字段值）建议在 DEF-009 执行前明确，但不影响本 ADR 决策质量，不阻断合并。
